# App (Streamlit dashboard) dependencies.
#
# Lower-bound (not exact) pins so prebuilt wheels resolve on whatever Python
# Streamlit Community Cloud provides (currently 3.14) — exact old pins have no
# 3.14 wheels and would compile from source (very slow / fails).
# xgboost is pinned to the version the model bundle was trained & pickled with.
#
# To RUN THE FULL PIPELINE (src/01..05) use:  pip install -r requirements-dev.txt

numpy>=2.1
pandas>=2.2
scikit-learn>=1.6            # required to unpickle the XGBClassifier bundle
xgboost==3.1.2               # match the trained model bundle (py3 wheel, any Python)
joblib>=1.3                  # load models/best_model_bundle.joblib
streamlit>=1.40             # app.py — 4-page dashboard + live prediction demo
