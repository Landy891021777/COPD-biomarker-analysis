# COPD Lung Gene-Expression Analysis — dependencies
# Tested on Python 3.13 (Anaconda).  Install with:  pip install -r requirements.txt

# --- Core data stack ---
numpy==2.1.3
pandas==2.2.3
scipy==1.15.3

# --- Machine learning ---
scikit-learn==1.6.1
xgboost==3.1.2
imbalanced-learn==0.13.0      # SMOTE for class imbalance (220 COPD / 108 CTRL)

# --- Batch correction ---
pycombat==0.20               # parametric ComBat (Johnson 2007) for platform batch effects

# --- Model interpretation ---
shap==0.50.0

# --- Visualization / dimensionality reduction ---
matplotlib==3.10.0
seaborn==0.13.2
umap-learn==0.5.12            # UMAP embedding for EDA

# --- GEO dataset retrieval (GSE47460 / GSE151052) ---
GEOparse==2.0.4

# --- Model persistence ---
joblib==1.4.2                # save/load trained model bundle (04 -> 05)

# --- Interactive dashboard ---
streamlit==1.45.1            # app.py — 4-page dashboard + live prediction demo

# --- Notebook / exploration ---
jupyter==1.1.1
ipykernel==6.29.5
