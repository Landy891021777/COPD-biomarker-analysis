# App (Streamlit dashboard) dependencies — kept minimal for fast, reliable
# cloud deployment (Streamlit Community Cloud reads this file).
#
# To RUN THE FULL PIPELINE (src/01..05) you need the heavier scientific stack:
#     pip install -r requirements-dev.txt
#
# Tested on Python 3.13.

numpy==2.1.3
pandas==2.2.3
scikit-learn==1.6.1          # required to unpickle the XGBClassifier bundle
xgboost==3.1.2               # trained model used by the live-prediction demo
joblib==1.4.2                # load models/best_model_bundle.joblib
streamlit==1.45.1            # app.py — 4-page dashboard + live prediction demo
